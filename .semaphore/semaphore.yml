version: v1.0
name: JavaScript Monorepo Bazel Build
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Install Bazel
    task:
      env_vars:
        - name: NODE_ENV
          value: test
        - name: CI
          value: 'true'
      prologue:
        commands:
          - checkout
          - node --version
          - npm --version
      jobs:
        - name: Install Bazel
          commands:
            - npm install -g @bazel/bazelisk
            - bazel --version
  - name: Install dependencies
    task:
      env_vars:
        - name: NODE_ENV
          value: test
        - name: CI
          value: 'true'
      prologue:
        commands:
          - checkout
          - node --version
          - npm --version
      jobs:
        - name: npm install
          commands:
            - npm install
  - name: Run Build
    task:
      env_vars:
        - name: NODE_ENV
          value: test
        - name: CI
          value: 'true'
      prologue:
        commands:
          - checkout
          - node --version
          - npm --version
      jobs:
        - name: npm build
          commands:
            - npm run build
