version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: 'Block #1'
    dependencies: []
    task:
      jobs:
        - name: 'Job #1'
          commands: []
  - name: Build
    dependencies:
      - 'Block #1'
    task:
      jobs:
        - name: Bazel Build Install Packages
          commands:
            - npm install
      prologue:
        commands:
          - checkout
          - nvm use
          - node --version
          - npm --version
